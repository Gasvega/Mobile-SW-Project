import { db } from "./firebaseConfig";
import { useEffect } from "react";
import React, { useState } from 'react';
import { View, TextInput, Button,StyleSheet,Text } from 'react-native';

var count = 1;

const[compareP, setcompareP] = useState('');

if(Answer == Quiz1_A-1) {
    <View>
        
        11 == true ? (<Button title = "confirmation"/>) : <Button title = "Retry" onPress = {()=> Enter}/>
    </View>
}
else if(Answer == Quiz1_B-1) {
    <View>
        <>You got one point</>
        11 == true ? () : <Button title = "Retry" onPress = "compareP"/>
    </View>
}
else if(Answer == Quiz1_C-1) {
    <View>
        <>You got one point</>
        11 == true ? () : <Button title = "Retry" onPress = ""/>
    </View>
}





export default compare